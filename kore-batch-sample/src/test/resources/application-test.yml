spring:
  batch:
    jdbc:
      initialize-schema: never
  liquibase:
    enabled: true
  jpa:
    open-in-view: false

batch:
  partitioning:
    grid-size: 1
    pool-size: 1

logging:
  level:
    dev.kore.batch: DEBUG
    org.springframework.batch: DEBUG
