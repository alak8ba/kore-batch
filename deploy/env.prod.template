# Template de configuration prod - copier vers /etc/kore-batch/env
# et renseigner les vraies valeurs (ce fichier ne doit PAS etre commite avec les vrais secrets)

# Base de donnees PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/batchdb
DB_USER=batch_user
DB_PASSWORD=CHANGER_MOI

# Spring
SPRING_PROFILES_ACTIVE=prod

# Fichier d entree (passe en parametre au lancement ou configure ici)
INPUT_FILE=/data/input/fichier.txt
